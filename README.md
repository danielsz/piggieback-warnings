# piggieback-warnings


1. Run `mvn clojure:nrepl`
2. pom.xml lists dependencies and nrepl middleware. Piggieback is
   included in both.
3. Change version string of nrepl from 1.6.0 to 1.5.2 and warnings disappear
